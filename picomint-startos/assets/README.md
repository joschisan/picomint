# Picomint StartOS assets

This directory is reserved for runtime assets that ship inside the
s9pk. Picomint's daemon binary lives in the Docker image, so for now
this directory only exists to satisfy the StartOS packager.
